@model string
    @if (Model.Contains("tồn"))
    {
        <span class="text-danger">@Model</span>
    }
    else if (Model.Contains("mới"))
    {
    <span class="text-success">@Model</span>
    }
    else
    {
        <span class="text-danger">@Model</span>
    }

