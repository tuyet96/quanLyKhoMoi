@model IEnumerable<QuanLyKho.Models.ModelDB.Loai_SP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_Loai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_Loai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Loai_Sp_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Loai_Sp_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Loai_Sp_Id })
        </td>
    </tr>
}

</table>
